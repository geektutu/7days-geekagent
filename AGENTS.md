## 我是谁

这是一个纯实验性质的 Agent 项目，希望用最简单的方式，实现一个 Agent。
对标 PI，OpenCode 等 Agent 的能力，但是以最简单的方式实现。

分天实现，每天实现一个功能。每天实现的功能，都是最简单的实现。
期望后续每天的功能，都在前一天的基础上新增，而非侵入式地修改。
每天的代码量控制在 500 行左右封顶，宁缺毋滥：某天预估会超 500 行，说明塞太多，砍掉或顺延到后续天。

## 提交

不要主动提交代码，等待我确认后，再提交。

commit message 统一用英文。

## doc 博客写作

写作风格参照「7天用Go从零实现」系列（https://geektutu.com/post/gee.html，已逐篇读 Gee / GeeCache / GeeRPC 的 Day1/Day2）。

### 写作目标

面向第一次接触 Agent 的读者，用费曼学习法循序渐进地讲清楚：它是什么、解决什么问题、主流方案如何处理、我们为什么这样实现。只呈现最终设计，不写踩坑或返工过程。

demo 用尽量少的代码把技术原理完整跑通，让读者能看懂、能复现；文章还要帮助读者建立技术地图，知道业界通常怎么做、相关概念叫什么，以及当前 demo 简化了什么。简洁是为了降低理解成本，不是为了追求篇幅短。

### 文章结构

标题使用 `Day N：<主题>`。正文沿用「为什么 → 目标 → 设计 → 实现 → 验证 → 没做 → 下一步」：

1. **为什么**：从初学者自然会问的问题或能直接感受到的痛点出发。先承认疑问合理，再用具体例子说明旧能力的边界和新能力的必要性；不要直接抛术语和结论。
2. **目标 / 验收标准**：只列能证明当天核心能力成立的 2-4 项结果。精简的是条目数量，不是信息量；每条应尽量包含触发条件、核心动作和可观察结果，并保留理解行为所需的关键参数或例子。不要把配色、按键细节、错误提示、管道降级、配置损坏处理、默认值、兼容性保持、类型检查等实现细节或可靠性分支包装成当天目标。结尾标明相对前一天的源码新增、删除和净增行数。
3. **设计**：先讲主流方案及优势，再说明 demo 的选择、适用规模和明确代价。不能只回答「我们怎么做」，还要回答「通常怎么做，为什么这次不那么做」。关键的必要性与取舍必须放在实现之前。
4. **实现**：先给命令和真实终端效果，让读者看到终点；再按执行顺序小步展示完整代码，每段后只解释与当天主题直接相关的要点。
5. **验证**：给出可复现命令、操作步骤和预期结果。
6. **没做**：列出当前实现的能力边界，为后续演进留出方向。
7. **下一步**：自然指向下一种能力，只描述方向；不写具体天数，也不提前点名尚未排期的功能。

### 方案对比

涉及成熟领域时，适当介绍主流方案、常见术语和理解主题所需的周边概念，不能为了精简全部删掉，也不能把 demo 的简化实现写成普遍最佳方案。

当两个或多个方案存在三个以上共同维度时，优先用表格比较本质、能力、成本、依赖和适用场景。表头应对应读者真正关心的问题，单元格保持短而具体；表格后只总结选择，不逐行重复。只有一两个简单差异时直接用短句。

### 代码与 demo

- 当天新增的文件和函数必须先完整展示，再按需摘出关键片段讲解，不能只贴零散代码。
- 每个源文件代码块的第一行用该语言的注释标注真实相对路径，如 `// day3/tools.ts`。JSON 等不支持注释的格式除外。路径注释是文章标注，不参与与仓库源码的逐字核对；除此之外，代码的 import、变量名、缩进和括号必须与仓库一致。
- 每段源码只讲核心流程和当天新增能力。`try/catch`、默认值、参数校验、空结果、重复注册、无变化写入等分支保留在完整代码里；除非直接关系核心能力、安全边界或验收标准，不单独展开。
- 终端 demo 必须符合真实运行行为，模型话术、工具结果、条目数、字符数和代码逻辑要能一一对应。
- 终端 demo 使用 `<pre style="background:#1e1e1e;color:#d4d4d4;padding:8px;border-radius:4px">`，每行使用 `<span style="color:#hex">整行内容</span>`，行间写真实换行，不用 `<br>`。颜色按 `color.ts` 的 ANSI 码：青 `#00cdcd`（36）、绿 `#00cd00`（32）、黄 `#cdcd00`（33）、灰 `#808080`（90）。Typst 由 `scripts/pdf.ts` 中 cmarker 的 `pre` / `span` 处理器渲染同一份 HTML。

### 文风

- 语言要活泼、自然、有交流感，但不靠夸张修辞制造气氛。优先使用具体场景、自然疑问、短句和主动动词，让节奏轻快。
- 口语化、结论先行，用第一人称「我/我们」带着读者一起实现，少用「你」。
- 新概念先用白话或必要的类比解释，再引入准确术语。类比必须帮助理解技术关系，用一次讲清即可；避免「生杀大权」「一锅端」「命里注定」等戏剧化表达，也不要让多个比喻连续代替准确说明。
- 由浅入深，不堆术语，不用 emoji。
- 同一个观点只讲一次：痛点放在「为什么」，方案和代价放在「设计」，实现部分讲代码如何工作。
- 删除不承载信息的过渡句和项目内部话术，如「老规矩」「先亮终点」「项目铁律」「收口」「零侵入」。不要反复喊「最简单」「零框架」「只增量不推翻」，让具体代码和取舍说明这些原则。
- 删掉重复、套话和偏离主题的细节，但保留必要的原理、方案对比和知识背景。

### 写完自审

把自己当成第一次读这个系列、准备照着复现的读者，逐项检查：

- **疑问**：是否回答了读者最可能先问的问题？有辨析价值的疑问是否被保留？
- **递进**：新概念是否建立在前文已有能力上？每一步是否说明了为什么需要下一步？
- **取舍**：是否介绍了主流方案、demo 选择、适用范围和代价？
- **知识面**：读者除了会写 demo，是否也获得了理解主题所需的术语和技术地图？
- **对比**：多个固定维度的比较是否适合改用表格？表格是否比段落更直观？
- **价值**：正文是否围绕原理和核心能力，而不是解释普通的防御性代码？
- **验收**：每条目标是否都能证明当天主题成立？是否写清“什么情况下，系统做什么，我们看到什么”，而不是只剩一个功能名？删掉某条若不影响读者理解当天交付，就把它移出验收标准。
- **一致性**：代码是否与仓库一致？行数、路径、命令和 demo 数字是否真实？
- **精炼**：删掉的是否只是重复和套话？删减后，初学者能否建立完整、准确的理解？
- **语气**：读起来是否自然、有节奏，但没有夸张比喻和刻意煽情？类比后是否及时回到准确术语？
