## 我是谁

这是一个纯实验性质的 Agent 项目，希望用最简单的方式，实现一个 Agent。
对标 PI，OpenCode 等 Agent 的能力，但是以最简单的方式实现。

分天实现，每天实现一个功能。每天实现的功能，都是最简单的实现。
期望后续每天的功能，都在前一天的基础上新增，而非侵入式地修改。
每天的代码量控制在 500 行左右封顶，宁缺毋滥：某天预估会超 500 行，说明塞太多，砍掉或顺延到后续天。

## 提交

不要主动提交代码，等待我确认后，再提交。

## doc 博客写作

写作风格参照「7天用Go从零实现」系列（https://geektutu.com/post/gee.html，已逐篇读 Gee / GeeCache / GeeRPC 的 Day1/Day2）。

**总原则：面向读者、循序渐进，用费曼学习法的姿态写。** 把自己当成在给入门读者讲课：从他已知的出发，每个概念先用大白话讲清「是什么、解决什么问题」，再一步步引出设计与代码，让他跟着节奏逐步进入状态。只呈现最终干净设计，不写踩坑/返工日志。

**标题**：保留 `Day N：<主题>` 的简洁风格（如现有 Day 1 / Day 3）；不强制加前缀。

**主线（day1 骨架，保证循序渐进）**：为什么 → 目标 → 设计 → 实现 → 验证 → 没做 → 下一步。
1. **为什么**：背景与动机，讲清痛点；可像 day3 先讲「裸做有什么坑」把必要性讲透。
2. **目标 / 验收标准**：bullet 列当天要做到的，结尾带**当天代码行数**，呼应 500 行封顶。
3. **设计**：先讲决策与权衡（为什么这样写），关键「必要性」小节放在实现前（day3 shell 裸跑范例）。
4. **实现：先效果，后实现**（geektutu 范式）：先给命令+终端输出让读者看到终点，再小步贴代码，每段后跟要点 bullet；标注真实文件路径（如 `day3/tools.ts`），变量名与仓库一致。
   - **新增代码必须完整展示**：当天新增的文件/函数要贴出完整代码，不要只截取片段或只言片语；若代码较长，可在完整展示后用 callout 高亮关键片段讲解，但完整版必须先行给出。
   - **代码块首行标注文件路径**：每一个贴出的源文件代码块，第一行必须是注释标明其在仓库中的真实相对路径（如 `// day3/tools.ts`）；JSON 等非注释语言代码块除外。这样读者能立刻对上仓库文件，也方便复制。
   - **终端效果 demo 双端上色**：展示终端效果时，整段用 `<pre style="background:#1e1e1e;color:#d4d4d4;padding:8px;border-radius:4px">` 包裹，逐行写成 `<span style="color:#hex">整行内容</span>`，行与行之间用真实换行（不用 `<br>`）。颜色必须对照 `color.ts` 的真实 ANSI 码取终端原色：青 `#00cdcd`（36）/ 绿 `#00cd00`（32）/ 黄 `#cdcd00`（33）/ 灰 `#808080`（90），切勿用粉彩色值。HTML 博客原生等宽+上色；Typst 侧由 `scripts/pdf.ts` 在 cmarker 的 `html` 字典里注册 `pre`（按行解析 span 上色、包成 `#1e1e1e` 深底终端块）与 `span` 处理器，单一写法两端通用。
5. **验证**：可复现步骤与预期输出。
6. **没做**：边界清单（day1 §6 / day3 §6 风格），为下一天铺垫。
7. **下一步**：自然指向下一天，只描述能力方向，**不提具体天数**，也不提前点名后续功能（如 `/model` 多模型切换这类还没排期的内容）。

**写完自审（读者视角）**：成稿后把自己当「第一次读这系列、想照着复现」的读者通读一遍，逐条自查：
- **术语自查**：删掉只有项目内部才懂的话术（「项目铁律」「收口」「零侵入」等），换成读者能直接感受的说法。
- **重复自查**：同一个观点只讲一次——痛点放在「为什么」，权衡放在「设计」，两个小节各司其职，不重复论证。
- **价值自查**：正文只保留帮助读者理解「为什么做、解决什么问题、核心设计如何工作」的内容。`try/catch`、默认值、参数校验、空结果、重复注册、无变化写入等防御性分支保留在完整代码里即可；除非它直接关系当天的核心能力、安全边界或验收标准，不单独设小节、列要点或包装成项目价值。
- **套话自查**：删除「老规矩」「先亮终点」「看到这个就知道」「动手前先交代」等不承载信息的过渡句，也不要反复强调「最简单」「零框架」「只增量不推翻」等项目口号；让具体问题、取舍和代码自行说明。
- **一致性自查**：贴出的代码与仓库**逐字一致**（缩进、括号、变量名、import 行、行数都核对），正文数字与代码/demo 对得上（「新增 N 行」要对准真实 diff，demo 里的条目数、字符数与代码行为一致）。
- **demo 真实性自查**：终端效果里的模型话术、工具返回结果要符合真实运行行为，与示例代码、验证环节一一对应。
- **人称自查**：全文少用「你」，多用「我/我们」，用第一人称把读者拉进来一起动手。

**文风**：口语化、第一人称「我/我们」（少用「你」，用「我们」把读者拉进来一起动手）、鼓励跟着写；不用 emoji、结论先行；费曼式讲解（新概念先类比铺垫、由浅入深、不堆术语）；强调「从零实现、最小可用、只增量不推翻」与项目「最简单」原则一致。
